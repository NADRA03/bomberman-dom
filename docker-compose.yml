version: '3.9'

services:
  bomberman-backend:
    build: .
    container_name: bomberman-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development